import React, {useState} from 'react';

const ReportBlankPage = ({firebase}) => {
    return (
        <h1>Please select a client on the left to view reports.</h1>
    )
}
export default ReportBlankPage;
